server:
  address: ":8080"
  port: 8080

database:
  host: localhost
  port: 3306
  user: root
  password: root123456
  dbname: ims_supply_chain
  charset: utf8mb4
  max_idle: 5
  max_open: 20

redis:
  host: localhost
  port: 6379
  password: ""
  db: 0

jwt:
  secret: "your-jwt-secret-key-change-in-production"
  access_expiry: 120  # minutes
  refresh_expiry: 7   # days

log:
  level: info
  output_path: logs/app.log
