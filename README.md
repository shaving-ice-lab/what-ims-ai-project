# 供应链订货系统 (IMS Supply Chain)

一个基于 Monorepo 架构的现代化供应链订货管理系统，支持多角色（管理员、供应商、门店）协同工作。

## ✨ 核心功能

- **门店端**：物料浏览、购物车管理、订单下单、市场行情查看
- **供应商端**：订单处理、物料价格管理、配送设置、统计分析
- **管理员端**：供应商/门店管理、物料管理、订单审核、系统配置
- **通用功能**：JWT认证、角色权限控制、实时通知、数据导出

## 🛠 技术栈

| 端 | 技术选型 |
|---|---------|
| **Web前端** | Next.js 16 + React + TypeScript + Ant Design |
| **移动端** | Taro 3.x + React + Taro UI |
| **后端** | Golang + Echo + GORM + MySQL 8.0 + Redis |
| **基础设施** | Turborepo + pnpm + Docker |

## 📁 项目结构

```text
.
├── apps/
│   ├── backend/       # Go后端服务
│   ├── web/           # Next.js Web应用
│   └── mobile/        # Taro移动端应用
├── packages/
│   ├── api-client/    # API客户端封装
│   ├── constants/     # 共享常量定义
│   ├── ui/            # 共享UI组件
│   └── utils/         # 共享工具函数
├── docs/              # 项目文档
├── turbo.json         # Turborepo配置
└── pnpm-workspace.yaml
```

## 🚀 快速开始

### 环境要求

- Node.js >= 18.0.0
- pnpm >= 8.0.0
- Go >= 1.21
- MySQL >= 8.0
- Redis >= 7.0

### 安装依赖

```bash
# 安装前端依赖
pnpm install

# 安装后端依赖
cd apps/backend && go mod download
```

### 环境配置

```bash
# 复制环境变量配置文件
cp .env.example .env
cp apps/backend/.env.example apps/backend/.env

# 修改配置文件中的数据库、Redis等连接信息
```

### 启动开发服务

```bash
# 启动所有前端服务
pnpm dev

# 启动后端服务
cd apps/backend && go run main.go

# 或者使用 turbo 同时启动
pnpm dev:all
```

### 各应用启动命令

| 应用 | 命令 | 端口 |
|-----|------|-----|
| Web前端 | `pnpm --filter web dev` | 3000 |
| 移动端H5 | `pnpm --filter mobile dev:h5` | 10086 |
| 后端API | `cd apps/backend && go run main.go` | 8080 |

### 构建

```bash
# 构建所有应用
pnpm build

# 构建指定应用
pnpm --filter web build
pnpm --filter mobile build
```

### 代码检查

```bash
# 运行所有检查
pnpm lint

# 格式化代码
pnpm format
```

## 📖 文档

- [技术选型报告](./docs/技术选型报告.md)
- [开发环境搭建指南](./docs/开发环境搭建指南.md)
- [系统设计文档](./docs/供应链系统设计文档.md)
- [开发任务列表](./docs/ims-development-todolist.md)
- [数据库设计](./docs/database/README.md)

## 🔧 开发规范

- **Git提交**: 遵循 [Conventional Commits](https://www.conventionalcommits.org/) 规范
- **代码风格**: ESLint + Prettier (前端), golangci-lint (后端)
- **分支管理**: main (生产) / develop (开发) / feature/* (功能)

## 📄 许可证

[MIT License](./LICENSE)
